# Description

This is one of "must-have" pet-project for many begginers in JS. It has basic functionality for every timer such as "start" "stop" and "pause" buttons.
Above that this timer has "lap" button which allows you to save the last result in order to compare several time results between each other.
